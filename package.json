{
  "name": "recsys-2027-check",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "test": "node --test",
    "seed:sql": "node scripts/generate-seed.mjs",
    "test:cloud": "node scripts/verify-cloud.mjs",
    "data:check": "node scripts/validate-catalog.mjs",
    "card:id": "node scripts/validate-catalog.mjs --id",
    "sync:cloud": "node scripts/sync-cloud.mjs",
    "build:check": "node scripts/check-dist.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  },
  "devDependencies": {
    "acorn": "^8.18.0",
    "vite": "^8.3.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
